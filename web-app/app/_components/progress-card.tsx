'use client'

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ProgressCard() {
  return (
    <Card className="flex mx-auto w-250 h-75 p-6 shadow">
    <CardHeader>
            <CardTitle>Today's Progress</CardTitle>
        </CardHeader>
        <CardContent>
        </CardContent>
    </Card>

  )
}
