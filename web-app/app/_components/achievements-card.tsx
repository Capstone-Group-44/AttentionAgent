'use-client'

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export function AchievementsCard(){
    return (
    <Card className="flex mx-auto w-250 h-50 p-6 shadow">
    <CardHeader>
            <CardTitle>Achievements</CardTitle>
        </CardHeader>
        <CardContent>
        </CardContent>
    </Card>

  )
}